import React, { useEffect, useMemo, useRef, useState } from 'react';
import { CheckCircle2, Copy, FileUp, Layers3, Lock, Plus, Save, X } from 'lucide-react';
import type { Competition, ExaminationSession } from './types';

type Slot = { id?:string; position:number; questionType:'single_choice'|'numeric_input'; optionCount:number; score:number; difficulty:string; topic:string; knowledgeSource:string; knowledgeRequirements:string; prohibitedKnowledge:string; assessmentIntent:string; estimatedSeconds:number; metadata:Record<string,unknown> };
type Version = { id:string; versionNumber:number; status:string; note:string; slotCount:number; slots?:Slot[]; lockedAt?:string };
type Blueprint = { id:string; name:string; competitionId:string; competitionName:string; sessionId:string; sessionName:string; roundName:string; subject:string; gradeOrCategory:string; language:string; description:string; metadataSchema:Record<string,unknown>; versions:Version[] };
type Props = { idToken?:string|null; userRole:string; competitions:Competition[]; sessions:ExaminationSession[]; onNavigate:(page:any,id?:string)=>void };
const LEVELS=['EASY','MEDIUM','HARD','VERY_HARD']; const labels:Record<string,string>={EASY:'Dễ',MEDIUM:'Trung bình',HARD:'Khó',VERY_HARD:'Rất khó',DRAFT:'Nháp',LOCKED:'Đã khóa',ARCHIVED:'Lưu trữ'};
const defaultDistribution=(total:number)=>{const easy=Math.floor(total*.2),hard=Math.floor(total*.2),very=Math.floor(total*.1);return {EASY:easy,MEDIUM:total-easy-hard-very,HARD:hard,VERY_HARD:very}};
const emptySlot=(position:number):Slot=>({position,questionType:'single_choice',optionCount:4,score:1,difficulty:'MEDIUM',topic:'',knowledgeSource:'',knowledgeRequirements:'',prohibitedKnowledge:'',assessmentIntent:'',estimatedSeconds:90,metadata:{}});
function apiFactory(token?:string|null){return async(path:string,options:RequestInit={})=>{const form=options.body instanceof FormData;const response=await fetch('/api/examination'+path,{...options,headers:{Authorization:`Bearer ${token||''}`,...(form?{}:{'Content-Type':'application/json'}),...(options.headers||{})}});const body=(response.headers.get('content-type')||'').includes('application/json')?await response.json():await response.blob();if(!response.ok){const detail=(body as any)?.error;const guidance=response.status===401?'Phiên đăng nhập đã hết. Hãy đăng nhập lại.':response.status===403?'Tài khoản của bạn chưa có quyền thực hiện thao tác này.':`Yêu cầu không thành công (mã ${response.status}). Hãy thử Đồng bộ lại; nếu vẫn lỗi, gửi mã này cho quản trị viên.`;throw new Error(detail||guidance);}return body as any;};}
const Field=({label,children}:{label:string;children:React.ReactNode})=><label className="block text-sm font-bold text-[#001e40]"><span className="mb-1 block">{label}</span>{children}</label>;
const Notice=({value,onClose}:{value:string;onClose:()=>void})=><div className="fixed bottom-5 right-5 z-50 max-w-md rounded-xl border border-blue-200 bg-white p-4 text-sm font-semibold text-[#001e40] shadow-xl">{value}<button onClick={onClose} className="ml-4 text-slate-500"><X className="h-4 w-4"/></button></div>;

export function BlueprintLibrary({idToken,userRole,competitions,sessions,onNavigate}:Props){
 const api=apiFactory(idToken),canManage=['ADMIN','MANAGER'].includes(userRole); const [rows,setRows]=useState<Blueprint[]>([]),[show,setShow]=useState(false),[notice,setNotice]=useState(''); const [form,setForm]=useState<any>({name:'',competitionId:'',sessionId:'',roundName:'',subject:'',gradeOrCategory:'',language:'Tiếng Việt',totalQuestions:20,difficultyDistribution:defaultDistribution(20),topics:'',questionType:'single_choice',optionCount:4,description:''});
 const load=async()=>{try{const data=await api('/blueprints');setRows(data.items||[])}catch(e:any){setNotice(e.message)}}; useEffect(()=>{load()},[]);
 const totalDifficulty=Object.values(form.difficultyDistribution).reduce((a:any,b:any)=>a+Number(b||0),0);
 const create=async(e:React.FormEvent)=>{e.preventDefault();if(totalDifficulty!==Number(form.totalQuestions)){setNotice('Tổng số slot theo độ khó chưa đúng.');return}try{const data=await api('/blueprints/draft-from-config',{method:'POST',body:JSON.stringify({...form,topics:form.topics.split(',').map((x:string)=>x.trim()).filter(Boolean)})});onNavigate('blueprint-detail',data.blueprint.id)}catch(err:any){setNotice(err.message)}};
 return <section className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-extrabold text-[#001e40]">Ma trận đề</h1><p className="mt-1 text-sm text-slate-600">Mọi đề được sinh từ một phiên bản ma trận đã khóa và các slot cố định.</p></div>{canManage&&<button onClick={()=>setShow(true)} className="ft-primary"><Plus className="h-4 w-4"/>Tạo ma trận nháp</button>}</div><div className="grid gap-4 lg:grid-cols-2">{rows.map(item=>{const locked=item.versions?.filter(x=>x.status==='LOCKED').length||0;const latest=item.versions?.[0];return <button key={item.id} onClick={()=>onNavigate('blueprint-detail',item.id)} className="rounded-2xl border border-slate-200 bg-white p-5 text-left shadow-sm transition hover:border-sky-300"><div className="flex items-start justify-between gap-3"><div><h2 className="font-extrabold text-[#001e40]">{item.name}</h2><p className="mt-1 text-sm text-slate-500">{item.competitionName||'Chưa gắn cuộc thi'} · {item.roundName||'Chưa chọn vòng'}</p></div><Layers3 className="h-5 w-5 text-sky-700"/></div><div className="mt-4 flex flex-wrap gap-2 text-xs font-bold"><span className="rounded-full bg-sky-50 px-2 py-1 text-sky-800">{item.subject||'Chưa có môn'} · {item.gradeOrCategory||'Chưa có khối/bảng'}</span><span className="rounded-full bg-slate-100 px-2 py-1">{item.versions?.length||0} phiên bản</span><span className="rounded-full bg-emerald-50 px-2 py-1 text-emerald-700">{locked} đã khóa</span>{latest&&<span className="rounded-full bg-amber-50 px-2 py-1 text-amber-700">v{latest.versionNumber}: {latest.slotCount} slot</span>}</div></button>})}</div>{!rows.length&&<div className="rounded-2xl border border-dashed p-12 text-center text-sm text-slate-500">Chưa có ma trận. Tạo từ wizard để hệ thống chuyển số câu và tỷ lệ thành slot trước khi sinh đề.</div>}
 {show&&<div className="ft-dialog-backdrop fixed inset-0 z-50 grid place-items-center p-4"><form onSubmit={create} className="ft-dialog-panel max-h-[calc(100vh-2rem)] w-full max-w-4xl overflow-y-auto bg-white p-6"><div className="flex justify-between gap-3"><div><h2 className="text-xl font-extrabold">Tạo ma trận nháp</h2><p className="mt-1 text-sm text-slate-600">Các tỷ lệ dưới đây chỉ tạo slot nháp để bạn kiểm tra; AI không tự quyết định tỷ lệ.</p></div><button type="button" onClick={()=>setShow(false)}><X className="h-5 w-5"/></button></div><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Tên ma trận *"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="ft-input"/></Field><Field label="Cuộc thi"><select value={form.competitionId} onChange={e=>setForm({...form,competitionId:e.target.value,sessionId:''})} className="ft-input"><option value="">Chọn cuộc thi</option>{competitions.map(x=><option key={x.id} value={x.id}>{x.code} · {x.name}</option>)}</select></Field><Field label="Kỳ thi"><select value={form.sessionId} onChange={e=>setForm({...form,sessionId:e.target.value})} className="ft-input"><option value="">Không gắn kỳ cụ thể</option>{sessions.filter(x=>!form.competitionId||x.competitionId===form.competitionId).map(x=><option key={x.id} value={x.id}>{x.code}: {x.name}</option>)}</select></Field><Field label="Vòng thi"><input value={form.roundName} onChange={e=>setForm({...form,roundName:e.target.value})} className="ft-input"/></Field><Field label="Môn"><input value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} className="ft-input"/></Field><Field label="Khối / bảng thi"><input value={form.gradeOrCategory} onChange={e=>setForm({...form,gradeOrCategory:e.target.value})} className="ft-input"/></Field><Field label="Tổng số câu"><input type="number" min="1" max="200" value={form.totalQuestions} onChange={e=>{const total=Math.max(1,Number(e.target.value)||1);setForm({...form,totalQuestions:total,difficultyDistribution:defaultDistribution(total)})}} className="ft-input"/></Field><Field label="Loại câu mặc định"><select value={form.questionType} onChange={e=>setForm({...form,questionType:e.target.value,optionCount:e.target.value==='numeric_input'?0:form.optionCount||4})} className="ft-input"><option value="single_choice">Trắc nghiệm một đáp án</option><option value="numeric_input">Điền đáp số</option></select></Field>{form.questionType==='single_choice'&&<Field label="Số phương án mặc định"><input type="number" min="2" max="8" value={form.optionCount} onChange={e=>setForm({...form,optionCount:Number(e.target.value)})} className="ft-input"/></Field>}<Field label="Chủ đề (phân cách bằng dấu phẩy)"><input value={form.topics} onChange={e=>setForm({...form,topics:e.target.value})} placeholder="Số học, Đại số, Hình học" className="ft-input"/></Field></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{LEVELS.map(level=><Field key={level} label={labels[level]}><input type="number" min="0" value={form.difficultyDistribution[level]} onChange={e=>setForm({...form,difficultyDistribution:{...form.difficultyDistribution,[level]:Number(e.target.value)}})} className="ft-input"/></Field>)}</div><p className={`mt-2 text-sm font-bold ${totalDifficulty===Number(form.totalQuestions)?'text-emerald-700':'text-rose-600'}`}>Tổng {totalDifficulty}/{form.totalQuestions} slot</p><Field label="Mô tả / yêu cầu bổ sung"><textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="ft-input mt-1 min-h-24"/></Field><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setShow(false)} className="rounded-lg border px-4 py-2 text-sm font-bold">Hủy</button><button disabled={totalDifficulty!==Number(form.totalQuestions)} className="ft-primary disabled:opacity-50">Tạo slot nháp</button></div></form></div>}{notice&&<Notice value={notice} onClose={()=>setNotice('')}/>}</section>
}

export function BlueprintEditor({idToken,userRole,onNavigate,blueprintId}:Props & {blueprintId:string}){
 const api=apiFactory(idToken),canManage=['ADMIN','MANAGER'].includes(userRole),fileRef=useRef<HTMLInputElement>(null);const [blueprint,setBlueprint]=useState<Blueprint|null>(null),[versionId,setVersionId]=useState(''),[slots,setSlots]=useState<Slot[]>([]),[busy,setBusy]=useState(''),[notice,setNotice]=useState('');
 const load=async()=>{try{const data=await api(`/blueprints/${blueprintId}`);setBlueprint(data);const version=data.versions?.find((x:Version)=>x.id===versionId)||data.versions?.[0];if(version){setVersionId(version.id);setSlots(version.slots||[])}}catch(e:any){setNotice(e.message)}};useEffect(()=>{load()},[blueprintId]); const version=useMemo(()=>blueprint?.versions.find(x=>x.id===versionId),[blueprint,versionId]); const editable=canManage&&version?.status==='DRAFT';
 const save=async()=>{if(!version)return;setBusy('save');try{const data=await api(`/blueprint-versions/${version.id}`,{method:'PUT',body:JSON.stringify({slots})});setSlots(data.slots||[]);await load();setNotice('Đã lưu các slot của phiên bản nháp.')}catch(e:any){setNotice(e.message)}finally{setBusy('')}};
 const lock=async()=>{if(!version||!window.confirm('Khóa phiên bản này? Sau đó muốn sửa phải tạo phiên bản mới.'))return;setBusy('lock');try{await api(`/blueprint-versions/${version.id}/lock`,{method:'POST',body:'{}'});await load();setNotice('Đã khóa phiên bản ma trận.')}catch(e:any){setNotice(e.message)}finally{setBusy('')}};
 const newVersion=async()=>{try{const data=await api(`/blueprints/${blueprintId}/versions`,{method:'POST',body:JSON.stringify({})});setVersionId(data.id);await load();setNotice('Đã tạo phiên bản nháp mới từ phiên bản gần nhất.')}catch(e:any){setNotice(e.message)}};
 const importFile=async(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file||!version)return;const body=new FormData();body.append('file',file);setBusy('import');try{const data=await api(`/blueprint-versions/${version.id}/import`,{method:'POST',body});setSlots(data.slots||[]);await load();setNotice('Đã import các slot từ Excel.')}catch(err:any){setNotice(err.message)}finally{setBusy('');if(fileRef.current)fileRef.current.value=''}};
 if(!blueprint)return <p className="p-8 text-slate-500">Đang tải ma trận đề...</p>;
 return <section className="space-y-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><button onClick={()=>onNavigate('blueprints')} className="text-sm font-bold text-[#1f4fc9]">← Quay lại Ma trận đề</button><h1 className="mt-3 text-3xl font-extrabold text-[#001e40]">{blueprint.name}</h1><p className="mt-1 text-sm text-slate-600">{blueprint.competitionName||'Chưa gắn cuộc thi'} · {blueprint.roundName||'Chưa chọn vòng'} · {blueprint.subject||'Chưa có môn'} · {blueprint.gradeOrCategory||'Chưa có khối/bảng'}</p></div><div className="flex flex-wrap gap-2"><select value={versionId} onChange={e=>{const next=blueprint.versions.find(x=>x.id===e.target.value);setVersionId(e.target.value);setSlots(next?.slots||[])}} className="ft-input max-w-40">{blueprint.versions.map(x=><option key={x.id} value={x.id}>v{x.versionNumber} · {labels[x.status]}</option>)}</select>{canManage&&<button onClick={newVersion} className="rounded-lg border px-3 py-2 text-sm font-bold"><Copy className="mr-1 inline h-4 w-4"/>Phiên bản mới</button>}{editable&&<button onClick={lock} disabled={!!busy} className="ft-primary"><Lock className="h-4 w-4"/>Khóa phiên bản</button>}</div></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-extrabold text-[#001e40]">Slot của phiên bản v{version?.versionNumber}</h2><p className="mt-1 text-sm text-slate-500">{editable?'Chỉnh sửa xong rồi khóa trước khi tạo đề.':'Phiên bản đã khóa; đề sinh từ đây sẽ giữ nguyên cấu trúc.'}</p></div>{editable&&<div className="flex gap-2"><input ref={fileRef} type="file" accept=".xlsx" className="hidden" onChange={importFile}/><button onClick={()=>fileRef.current?.click()} disabled={!!busy} className="rounded-lg border px-3 py-2 text-sm font-bold"><FileUp className="mr-1 inline h-4 w-4"/>Import Excel</button><button onClick={()=>setSlots([...slots,emptySlot(slots.length+1)])} className="rounded-lg border px-3 py-2 text-sm font-bold text-[#1f4fc9]"><Plus className="mr-1 inline h-4 w-4"/>Thêm slot</button><button onClick={save} disabled={!!busy} className="ft-primary"><Save className="h-4 w-4"/>Lưu</button></div>}</div><div className="mt-4 overflow-x-auto"><table className="ft-table min-w-[1250px]"><thead><tr><th>Vị trí</th><th>Loại câu</th><th>PA</th><th>Điểm</th><th>Độ khó</th><th>Chủ đề</th><th>Nguồn / yêu cầu kiến thức</th><th>Không sử dụng</th><th>Assessment Intent</th><th>Giây</th>{editable&&<th/>}</tr></thead><tbody>{slots.map((slot,index)=><tr key={slot.id||index}><td>{editable?<input type="number" min="1" value={slot.position} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,position:Number(e.target.value)}:x))} className="w-16 rounded border p-1"/>:slot.position}</td><td>{editable?<select value={slot.questionType} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,questionType:e.target.value as any,optionCount:e.target.value==='numeric_input'?0:x.optionCount}:x))} className="rounded border p-1"><option value="single_choice">Trắc nghiệm</option><option value="numeric_input">Đáp số</option></select>:slot.questionType}</td><td>{editable?<input type="number" min="0" max="8" disabled={slot.questionType==='numeric_input'} value={slot.optionCount} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,optionCount:Number(e.target.value)}:x))} className="w-14 rounded border p-1"/>:slot.optionCount||'—'}</td><td>{editable?<input type="number" min="0.01" step="0.25" value={slot.score} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,score:Number(e.target.value)}:x))} className="w-16 rounded border p-1"/>:slot.score}</td><td>{editable?<select value={slot.difficulty} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,difficulty:e.target.value}:x))} className="rounded border p-1">{LEVELS.map(x=><option key={x}>{x}</option>)}</select>:labels[slot.difficulty]}</td><td>{editable?<input value={slot.topic} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,topic:e.target.value}:x))} className="w-36 rounded border p-1"/>:slot.topic||'—'}</td><td>{editable?<textarea value={slot.knowledgeRequirements} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,knowledgeRequirements:e.target.value}:x))} className="w-48 rounded border p-1"/>:slot.knowledgeRequirements||'—'}</td><td>{editable?<textarea value={slot.prohibitedKnowledge} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,prohibitedKnowledge:e.target.value}:x))} className="w-40 rounded border p-1"/>:slot.prohibitedKnowledge||'—'}</td><td>{editable?<textarea value={slot.assessmentIntent} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,assessmentIntent:e.target.value}:x))} className="w-48 rounded border p-1"/>:slot.assessmentIntent||'—'}</td><td>{editable?<input type="number" min="1" value={slot.estimatedSeconds} onChange={e=>setSlots(slots.map((x,i)=>i===index?{...x,estimatedSeconds:Number(e.target.value)}:x))} className="w-16 rounded border p-1"/>:slot.estimatedSeconds}</td>{editable&&<td><button onClick={()=>setSlots(slots.filter((_,i)=>i!==index).map((x,i)=>({...x,position:i+1})))} className="text-rose-600">Xóa</button></td>}</tr>)}</tbody></table></div></section>{notice&&<Notice value={notice} onClose={()=>setNotice('')}/>}</section>
}