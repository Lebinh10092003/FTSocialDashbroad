import React,{useState}from 'react';
import {ArrowLeft,Pencil}from 'lucide-react';
import type {Candidate}from './types';
type Tab='info'|'students'|'attendance';
export default function ClassDetail({data,candidates,onBack,onCandidate}:{data:{name:string;teacher:string;schedule:string};candidates:Candidate[];onBack:()=>void;onCandidate:(c:Candidate)=>void}){
 const[editing,setEditing]=useState(false),[tab,setTab]=useState<Tab>('info'),[attendance,setAttendance]=useState<Record<string,string>>({});
 const next=(id:string)=>setAttendance(x=>({...x,[id]:x[id]==='Có mặt'?'Vắng có phép':x[id]==='Vắng có phép'?'Vắng':'Có mặt'}));
 const button=(id:Tab,label:string)=><button onClick={()=>setTab(id)} className={`border-b-2 pb-2 text-sm font-bold ${tab===id?'border-[#aa3000] text-[#aa3000]':'border-transparent text-slate-600'}`}>{label}</button>;
 const rows=candidates.slice(0,2);
 return <><button onClick={onBack} className="mb-4 inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft className="h-4 w-4"/>Quay lại các lớp ôn tập</button><section className="ft-surface"><div className="flex justify-between gap-4"><div><p className="text-sm font-bold text-[#aa3000]">LỚP ÔN TẬP</p><h1 className="mt-1 text-3xl font-extrabold">{data.name}</h1><p className="mt-2 text-sm text-slate-600">Giáo viên: {data.teacher} · {data.schedule}</p></div><button onClick={()=>setEditing(!editing)} className="inline-flex h-fit items-center gap-2 rounded-lg border px-4 py-2 text-sm font-bold"><Pencil className="h-4 w-4"/>Thay đổi thông tin</button></div><div className="mt-6 flex gap-5 border-t pt-4">{button('info','Thông tin lớp học')}{button('students','Danh sách học sinh')}{button('attendance','Danh sách điểm danh')}</div></section>{editing&&<section className="mt-6 ft-surface"><h2 className="text-xl font-bold">Chỉnh sửa thông tin lớp</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label>Tên lớp<input defaultValue={data.name} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label>Giáo viên<input defaultValue={data.teacher} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label>Thời gian bắt đầu<input defaultValue="01/07/2026" className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label>Thời gian kết thúc<input defaultValue="08/08/2026" className="mt-1 w-full rounded-lg border px-3 py-2"/></label></div></section>}{tab==='info'&&<div className="mt-6 grid gap-5 lg:grid-cols-3"><section className="ft-surface lg:col-span-2"><h2 className="text-xl font-bold">Thời khóa biểu</h2><p className="mt-1 text-sm text-slate-500">Một lớp có thể học nhiều khung giờ trong tuần.</p><table className="mt-5 w-full border-collapse text-sm"><thead><tr><th className="border bg-slate-50 p-3">Khung giờ</th><th className="border bg-slate-50 p-3">Thứ 3</th><th className="border bg-slate-50 p-3">Thứ 5</th><th className="border bg-slate-50 p-3">Thứ 7</th></tr></thead><tbody><tr><td className="border p-3 font-semibold">19:00 – 20:30</td><td className="border p-3 text-center">Ôn tập</td><td className="border p-3 text-center">Ôn tập</td><td className="border p-3 text-center">Ôn tập</td></tr></tbody></table></section><section className="ft-surface"><h2 className="text-xl font-bold">Thông tin lớp</h2><p className="mt-5 text-sm"><b>Giáo viên</b><br/>{data.teacher}</p><p className="mt-4 text-sm"><b>Thời gian</b><br/>01/07/2026 — 08/08/2026</p></section></div>}{tab==='students'&&<section className="mt-6 ft-surface overflow-hidden"><h2 className="mb-4 text-xl font-bold">Danh sách học sinh</h2><table className="ft-table"><thead><tr><th>Học viên</th><th>Trường học</th><th>Các cuộc thi đã tham gia</th></tr></thead><tbody>{rows.map(c=><tr key={c.code} onClick={()=>onCandidate(c)} className="cursor-pointer hover:bg-blue-50/50"><td><b>{c.name}</b><p className="text-xs text-slate-500">{c.code}</p></td><td>{c.school}</td><td>{c.contests}</td></tr>)}</tbody></table></section>}{tab==='attendance'&&<section className="mt-6 ft-surface overflow-hidden"><h2 className="mb-4 text-xl font-bold">Danh sách điểm danh</h2><table className="ft-table"><thead><tr><th>Học viên</th><th>Trường học</th><th>Điểm danh buổi gần nhất</th><th></th></tr></thead><tbody>{rows.map(c=>{const status=attendance[c.code]||'Có mặt';return <tr key={c.code}><td><b>{c.name}</b><p className="text-xs text-slate-500">{c.code}</p></td><td>{c.school}</td><td><span className={`rounded-full px-2 py-1 text-xs font-bold ${status==='Có mặt'?'bg-emerald-50 text-emerald-700':status==='Vắng có phép'?'bg-amber-50 text-amber-700':'bg-rose-50 text-rose-700'}`}>{status}</span></td><td><button onClick={()=>next(c.code)} className="text-sm font-bold text-[#aa3000]">Cập nhật</button></td></tr>})}</tbody></table></section>}</>}
